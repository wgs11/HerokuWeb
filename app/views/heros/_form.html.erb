<%= form_with(model: hero, local: true) do |form| %>
  <% if hero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hero.errors.count, "error") %> prohibited this hero from being saved:</h2>

      <ul>
      <% hero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :hero_name %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.number_field :total, id: :hero_total %>
  </div>

  <div class="field">
    <%= form.label :unlocks %>
    <%= form.number_field :unlocks, id: :hero_unlocks %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url, id: :hero_image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
